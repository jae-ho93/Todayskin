import type { EvidenceGrade } from '../types';

/**
 * F69: 근거 등급의 뜻을 화면에서 쓰는 말로 적어 둔다.
 *
 * 배지에는 `A`·`B`·`C` 한 글자와 짧은 라벨만 들어가는데, 그것만 보면 B를 "A보다
 * 조금 약한 근거"로 읽게 된다. 실제로는 B에 인용할 문헌이 아예 없다 — 강도의
 * 차이가 아니라 종류의 차이다. "왜 이 등급인가요?"에서 이 설명을 보여준다.
 */
export const GRADE_CRITERIA: Record<EvidenceGrade, string> = {
  A: '공공기관·국제기구가 발행한 문서를 근거로 합니다. 아래 근거 문서에서 원문을 직접 확인할 수 있어요.',
  B: '내 진단 사진과 그날 날씨를 바탕으로 AI가 쓴 문장이에요. 참고할 만한 제안이지만 인용한 문헌은 없어요.',
  C: '내가 남긴 기록의 시간에 따른 변화를 통계로 본 것이에요. 함께 나타나는 경향일 뿐 원인과 결과는 아니에요.',
};

/** 인용 문헌이 없는 추천에 붙는 안내. 출처 칸을 비워두면 누락으로 읽힌다. */
export const NO_SOURCE_NOTICE = '인용한 문헌은 없어요';

/** 추천 화면에서 상시 노출하는 고지. 약관에만 두면 화면에서는 보이지 않는다. */
export const MEDICAL_DISCLAIMER =
  '참고용 정보이며 의료 진단이 아닙니다. 피부 이상이 지속되면 의료기관에서 진료를 받으세요.';
