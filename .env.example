# 실기기(Expo Go)에서 로컬 백엔드에 접속하려면, 아래 값을 자신의 PC LAN IP로 바꿔서
# ".env" 파일로 복사해 사용하세요 (예: ipconfig / ifconfig 로 확인한 IPv4 주소).
# 시뮬레이터/웹에서만 테스트한다면 이 값을 그대로 둬도 됩니다 (localhost 사용).
EXPO_PUBLIC_API_BASE_URL=http://192.168.0.10:8000
