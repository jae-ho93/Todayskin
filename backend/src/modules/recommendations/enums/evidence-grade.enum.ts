/**
 * 근거 등급 — 서버가 고정하며 LLM이 결정하지 않는다.
 *
 * - A: 공인 가이드라인. evidence-sources.ts에 등록된 실제 문서를 가리킨다.
 * - B: AI 생성. 사용자 사진과 그날 날씨로 만든 문장이라 인용할 문헌이 없다.
 * - C: 개인 시계열 통계적 관찰. 사용자 본인 데이터가 근거이고 외부 인용은 없다.
 *
 * N45: B를 원래 "개별 임상/관찰 연구"라고 정의했는데, 실제로 하는 일은 LLM 생성이라
 * 연구를 근거로 든 적이 없다. 등급 정의가 동작과 다르면 화면 문구도 따라서 틀린다.
 */
export enum EvidenceGrade {
  A = 'A',
  B = 'B',
  C = 'C',
}
