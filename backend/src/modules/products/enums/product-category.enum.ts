/**
 * 제품 카테고리 — Product.category와 1:1 대응.
 * moisture(보습), elasticity(탄력), brightening(미백), barrier(장벽 강화).
 */
export enum ProductCategory {
  MOISTURE = 'moisture',
  ELASTICITY = 'elasticity',
  BRIGHTENING = 'brightening',
  BARRIER = 'barrier',
}
