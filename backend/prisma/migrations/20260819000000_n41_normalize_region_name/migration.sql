-- N41: 시/도 표기 정규화
--
-- 스냅샷에 '부산'과 '부산광역시'가 섞여 저장돼 있었다. 표기가 갈리면 지역별 집계가
-- 두 그룹으로 나뉜다. 현재 코드는 세 수집 분기 모두 근사표의 정식 명칭을 쓰므로
-- 새로 생기지는 않는다 — 남은 레거시 행만 합친다.
--
-- N39의 자외선 시각과 달리 이 보정은 안전하다. '부산'이 정답인 경우가 없고,
-- 정규화가 다른 정보를 지우지도 않는다.
UPDATE "weather_snapshots" SET "region_name" = '부산광역시' WHERE "region_name" = '부산';
